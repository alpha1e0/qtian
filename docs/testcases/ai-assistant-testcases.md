# AI助手 功能测试用例

> 模块：AI助手（AiAssistantPage / RoleplayChat）
> 日期：2026-04-01
> 状态：待Review

---

## TC-01 入口与导航

### TC-01-01 首页搜索进入AI助手

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用已启动，位于首页 |
| **操作步骤** | 1. 在首页搜索框输入"什么是量子计算"<br>2. 点击搜索按钮（或按Enter键） |
| **预期结果** | 页面跳转到AI助手页面，搜索内容自动填入输入框 |

### TC-01-02 首页常用命令进入AI助手

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用已启动，位于首页 |
| **操作步骤** | 1. 点击常用命令区域中的"翻译这段文字"标签 |
| **预期结果** | 页面跳转到AI助手页面，"翻译这段文字"自动填入输入框 |

### TC-01-03 首页工具卡片进入AI助手

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用已启动，位于首页 |
| **操作步骤** | 1. 点击常用工具区域的"AI助手"卡片 |
| **预期结果** | 页面跳转到AI助手页面，显示完整的聊天界面 |

### TC-01-04 首页搜索框清除

| 项目 | 内容 |
|------|------|
| **前置条件** | 应用已启动，搜索框中已输入文本 |
| **操作步骤** | 1. 点击搜索框右侧的 X 清除按钮 |
| **预期结果** | 搜索框文本被清空 |

---

## TC-02 侧边栏 - 场景选择

### TC-02-01 选择已有场景

| 项目 | 内容 |
|------|------|
| **前置条件** | AI助手页面已打开，存在至少一个场景配置 |
| **操作步骤** | 1. 点击"场景"下拉框<br>2. 选择其中一个场景 |
| **预期结果** | 下拉框显示所选场景名称；"对话历史"下拉框出现并加载该场景下的历史记录 |

### TC-02-02 切换场景

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择场景A，场景A下有对话历史 |
| **操作步骤** | 1. 点击"场景"下拉框<br>2. 切换为场景B |
| **预期结果** | 对话历史列表更新为场景B的历史；聊天区域清空 |

### TC-02-03 未选择场景时界面状态

| 项目 | 内容 |
|------|------|
| **前置条件** | AI助手页面已打开 |
| **操作步骤** | 1. 不选择任何场景 |
| **预期结果** | "对话历史"区域不显示；聊天区域为空；发送按钮不可用 |

### TC-02-04 打开场景编辑器

| 项目 | 内容 |
|------|------|
| **前置条件** | AI助手页面已打开 |
| **操作步骤** | 1. 点击场景旁的"编辑"按钮 |
| **预期结果** | 弹出场景编辑器弹窗；左侧显示场景列表；右侧显示JSON编辑和系统提示词两种编辑模式 |

### TC-02-05 场景编辑器 - 新建场景

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景编辑器已打开 |
| **操作步骤** | 1. 点击"+ 新建场景"按钮<br>2. 在弹窗中输入场景名称，如"测试助手"<br>3. 点击"创建" |
| **预期结果** | 新场景"测试助手"出现在场景列表中；右侧编辑区加载默认模板 |

### TC-02-06 场景编辑器 - 新建场景名称为空

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景编辑器已打开 |
| **操作步骤** | 1. 点击"+ 新建场景"按钮<br>2. 不输入名称，直接点击"创建" |
| **预期结果** | 提示"请输入场景名称"；场景不被创建 |

### TC-02-07 场景编辑器 - JSON编辑模式保存

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景编辑器已打开，已选中一个场景，处于JSON编辑模式 |
| **操作步骤** | 1. 修改JSON内容中的`name`字段<br>2. 点击"保存" |
| **预期结果** | 提示保存成功；场景列表中名称同步更新 |

### TC-02-08 场景编辑器 - 系统提示词模式保存

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景编辑器已打开，已选中一个场景，处于系统提示词编辑模式 |
| **操作步骤** | 1. 编辑Markdown格式的系统提示词内容<br>2. 点击"保存" |
| **预期结果** | 提示保存成功；对应的角色Markdown文件已更新 |

### TC-02-09 场景编辑器 - 保存无效JSON

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景编辑器已打开，处于JSON编辑模式 |
| **操作步骤** | 1. 将JSON内容改为非法格式（如删除一个花括号）<br>2. 点击"保存" |
| **预期结果** | 提示保存失败，显示错误信息；原数据不被覆盖 |

### TC-02-10 场景编辑器 - 关闭

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景编辑器已打开 |
| **操作步骤** | 1. 点击"关闭"按钮 |
| **预期结果** | 场景编辑器弹窗关闭；返回聊天界面 |

---

## TC-03 侧边栏 - LLM模型选择

### TC-03-01 选择LLM模型

| 项目 | 内容 |
|------|------|
| **前置条件** | AI助手页面已打开，存在至少一个LLM配置 |
| **操作步骤** | 1. 点击"LLM模型"下拉框<br>2. 选择一个模型配置 |
| **预期结果** | 下拉框显示所选模型名称；后续聊天使用该模型 |

### TC-03-02 切换LLM模型

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型A，已存在对话历史 |
| **操作步骤** | 1. 点击"LLM模型"下拉框<br>2. 切换为模型B |
| **预期结果** | 模型切换成功；聊天服务使用新模型重新初始化；已有对话记录保留 |

### TC-03-03 无LLM配置可用

| 项目 | 内容 |
|------|------|
| **前置条件** | assistant/llm/ 目录下无配置文件 |
| **操作步骤** | 1. 打开AI助手页面<br>2. 查看"LLM模型"下拉框 |
| **预期结果** | 下拉框为空；发送消息时提示配置缺失 |

---

## TC-04 侧边栏 - 对话历史管理

### TC-04-01 新建对话历史

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择场景 |
| **操作步骤** | 1. 点击"+ 新建"按钮<br>2. 在弹窗中输入名称，如"测试对话1"<br>3. 点击"创建" |
| **预期结果** | 新历史"测试对话1"出现在历史下拉框中并被选中；聊天区域为空 |

### TC-04-02 新建对话历史 - 名称为空

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择场景 |
| **操作步骤** | 1. 点击"+ 新建"按钮<br>2. 不输入名称，直接点击"创建" |
| **预期结果** | 提示"请输入对话历史名称"；对话不被创建 |

### TC-04-03 选择已有对话历史

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择场景，该场景下存在多个对话历史 |
| **操作步骤** | 1. 点击"对话历史"下拉框<br>2. 选择其中一个历史 |
| **预期结果** | 聊天区域加载该历史的所有消息；消息按时间顺序正确显示 |

### TC-04-04 切换对话历史

| 项目 | 内容 |
|------|------|
| **前置条件** | 已在历史A中有对话内容 |
| **操作步骤** | 1. 切换到历史B |
| **预期结果** | 聊天区域更新为历史B的内容；历史A的内容已自动保存 |

---

## TC-05 聊天 - 发送消息

### TC-05-01 发送普通文本消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型、场景、对话历史 |
| **操作步骤** | 1. 在输入框输入"你好"<br>2. 点击"发送 (Ctrl+Enter)"按钮 |
| **预期结果** | 用户消息"你好"显示在右侧（蓝色背景）；AI开始流式回复；AI消息显示在左侧（绿色背景）；消息自动滚动到最新位置 |

### TC-05-02 Ctrl+Enter快捷键发送

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型、场景、对话历史 |
| **操作步骤** | 1. 在输入框输入"测试消息"<br>2. 按下 Ctrl+Enter |
| **预期结果** | 消息发送成功，行为与点击发送按钮一致 |

### TC-05-03 发送空消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型、场景、对话历史 |
| **操作步骤** | 1. 输入框为空<br>2. 点击"发送"按钮 |
| **预期结果** | 消息不被发送；输入框无变化 |

### TC-05-04 发送多行文本消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型、场景、对话历史 |
| **操作步骤** | 1. 在输入框中输入多行文本（使用Enter换行）<br>2. 按 Ctrl+Enter 发送 |
| **预期结果** | 多行文本正确显示在消息气泡中；AI能正确理解和回复多行内容 |

### TC-05-05 发送包含代码的消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型、场景、对话历史 |
| **操作步骤** | 1. 输入包含代码块的消息，如"请写一个Python hello world"<br>2. 发送 |
| **预期结果** | AI回复中的代码块被正确渲染（语法高亮、代码块样式） |

### TC-05-06 发送消息时加载状态

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型、场景、对话历史 |
| **操作步骤** | 1. 发送一条消息<br>2. 观察发送按钮状态 |
| **预期结果** | 发送中按钮显示loading状态；输入框不可操作；AI回复完成后恢复正常 |

### TC-05-07 未选择场景时发送消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型，未选择场景 |
| **操作步骤** | 1. 在输入框输入文本<br>2. 点击发送 |
| **预期结果** | 提示需要先选择场景和历史；消息不被发送 |

### TC-05-08 未选择对话历史时发送消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择模型和场景，未选择/创建对话历史 |
| **操作步骤** | 1. 在输入框输入文本<br>2. 点击发送 |
| **预期结果** | 提示需要先创建或选择对话历史；消息不被发送 |

---

## TC-06 聊天 - 流式响应

### TC-06-01 正常流式输出

| 项目 | 内容 |
|------|------|
| **前置条件** | 已完成场景和历史选择，LLM服务正常 |
| **操作步骤** | 1. 发送一条问题，如"介绍一下自己"<br>2. 观察AI回复过程 |
| **预期结果** | AI回复逐字/逐段流式显示；消息区域自动滚动跟随新内容；流式完成后消息完整显示 |

### TC-06-02 流式输出中的Markdown渲染

| 项目 | 内容 |
|------|------|
| **前置条件** | 已完成配置 |
| **操作步骤** | 1. 发送"请用Markdown格式列出3个编程语言的特点"<br>2. 观察流式输出过程 |
| **预期结果** | 流式输出过程中Markdown格式逐步渲染（标题、列表、加粗等）；最终渲染结果正确 |

### TC-06-03 流式中断（网络异常）

| 项目 | 内容 |
|------|------|
| **前置条件** | 已完成配置，AI正在流式回复 |
| **操作步骤** | 1. 发送消息<br>2. 在AI回复过程中断开网络连接 |
| **预期结果** | 流式输出停止；已接收的内容保留在聊天区域；显示错误提示"聊天出错"或类似信息 |

---

## TC-07 聊天 - 回退与重新生成

### TC-07-01 回退最后一条消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有至少一轮对话（用户消息+AI回复） |
| **操作步骤** | 1. 点击"回退"按钮 |
| **预期结果** | 最后一轮对话（用户消息+AI回复）被移除；聊天区域更新；历史文件同步更新 |

### TC-07-02 回退多条消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有3轮以上对话 |
| **操作步骤** | 1. 连续点击"回退"按钮3次 |
| **预期结果** | 每次点击移除最后一轮对话；3次后最新3轮对话被移除 |

### TC-07-03 空对话时回退

| 项目 | 内容 |
|------|------|
| **前置条件** | 对话历史为空（无消息） |
| **操作步骤** | 1. 点击"回退"按钮 |
| **预期结果** | 按钮处于禁用状态，无法点击 |

### TC-07-04 重新生成AI回复

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有至少一轮对话（用户消息+AI回复） |
| **操作步骤** | 1. 点击"重新生成"按钮 |
| **预期结果** | 最后一条AI回复被移除；AI使用相同的对话上下文重新生成回复；新回复流式显示 |

### TC-07-05 重新生成 - 空对话

| 项目 | 内容 |
|------|------|
| **前置条件** | 对话历史为空 |
| **操作步骤** | 1. 点击"重新生成"按钮 |
| **预期结果** | 按钮处于禁用状态，无法点击 |

### TC-07-06 重新生成 - AI正在回复中

| 项目 | 内容 |
|------|------|
| **前置条件** | AI正在流式回复 |
| **操作步骤** | 1. 观察按钮状态 |
| **预期结果** | "回退"和"重新生成"按钮应处于禁用状态 |

---

## TC-08 聊天 - Agent模式

### TC-08-01 Agent模式 - 工具调用流程

| 项目 | 内容 |
|------|------|
| **前置条件** | 已选择Agent模式的场景（is_agent=true），场景配置了工具 |
| **操作步骤** | 1. 发送需要工具调用的指令，如"查看当前目录下的文件"<br>2. 观察AI回复过程 |
| **预期结果** | AI决定调用工具（如shell_execute）；聊天区域显示可折叠的工具调用面板，包含工具名称、参数（JSON格式）；工具执行后显示结果；AI基于结果继续回复 |

### TC-08-02 Agent模式 - 工具调用面板展开/折叠

| 项目 | 内容 |
|------|------|
| **前置条件** | Agent模式下已有工具调用记录 |
| **操作步骤** | 1. 点击工具调用面板的折叠区域<br>2. 再次点击 |
| **预期结果** | 第一次点击展开，显示详细的工具参数和执行结果；第二次点击折叠，只显示工具名称摘要 |

### TC-08-03 Agent模式 - 工具执行错误

| 项目 | 内容 |
|------|------|
| **前置条件** | Agent模式下，AI调用了执行会失败的工具 |
| **操作步骤** | 1. 发送指令触发一个会失败的工具调用（如执行不存在的命令） |
| **预期结果** | 工具调用面板显示错误状态（红色高亮）；错误信息显示在结果区域；AI收到错误信息后继续处理 |

### TC-08-04 Agent模式 - 停止执行

| 项目 | 内容 |
|------|------|
| **前置条件** | Agent模式正在执行工具调用链 |
| **操作步骤** | 1. 在AI执行工具期间点击"停止"按钮 |
| **预期结果** | 工具执行被中止；已完成的工具调用结果保留在聊天区域；AI停止后续操作 |

### TC-08-05 Agent模式 - 多轮工具调用

| 项目 | 内容 |
|------|------|
| **前置条件** | Agent模式，场景配置了工具 |
| **操作步骤** | 1. 发送复杂指令，如"查看package.json的内容，然后告诉我项目用了哪些依赖"<br>2. 观察AI行为 |
| **预期结果** | AI可能进行多轮工具调用（读取文件→分析内容）；每轮调用都有对应的面板；最终给出综合回复 |

### TC-08-06 Agent模式 - 最大工具轮次限制

| 项目 | 内容 |
|------|------|
| **前置条件** | Agent模式 |
| **操作步骤** | 1. 构造一个可能导致无限循环的场景（如让AI反复调用工具） |
| **预期结果** | 工具调用达到最大轮次（10轮）后自动停止；显示警告提示 |

---

## TC-09 聊天标签页 - 编辑对话

### TC-09-01 切换到编辑对话标签页

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有对话内容 |
| **操作步骤** | 1. 点击"编辑对话"标签页 |
| **预期结果** | 显示对话历史的文本编辑区域，格式为`role|name\ncontent\n---\n`；内容与当前对话一致 |

### TC-09-02 编辑对话内容并保存

| 项目 | 内容 |
|------|------|
| **前置条件** | 已在编辑对话标签页 |
| **操作步骤** | 1. 修改某条消息的内容<br>2. 点击"保存修改" |
| **预期结果** | 保存成功；切换回"对话"标签页后，聊天区域显示更新后的消息 |

### TC-09-03 编辑对话 - 添加新消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已在编辑对话标签页 |
| **操作步骤** | 1. 在编辑区末尾添加新消息，格式：`user|User\n这是一条新消息\n---\n`<br>2. 点击"保存修改" |
| **预期结果** | 保存成功；新消息出现在聊天区域 |

### TC-09-04 编辑对话 - 保存格式错误

| 项目 | 内容 |
|------|------|
| **前置条件** | 已在编辑对话标签页 |
| **操作步骤** | 1. 删除分隔符`---`使格式不正确<br>2. 点击"保存修改" |
| **预期结果** | 提示保存失败或格式错误信息；原有数据不被破坏 |

---

## TC-10 聊天标签页 - 原始数据

### TC-10-01 查看原始JSON数据

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有对话内容 |
| **操作步骤** | 1. 点击"原始数据"标签页 |
| **预期结果** | 显示当前对话历史的完整JSON数据；格式化缩进为2空格；数据结构与聊天消息一致 |

### TC-10-02 原始数据与对话同步

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有对话内容 |
| **操作步骤** | 1. 在"对话"标签页发送一条新消息<br>2. 切换到"原始数据"标签页 |
| **预期结果** | JSON数据包含最新发送的消息 |

---

## TC-11 消息持久化

### TC-11-01 对话自动保存

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有对话在进行中 |
| **操作步骤** | 1. 发送一条消息并收到AI回复<br>2. 切换到其他对话历史<br>3. 再切换回当前对话 |
| **预期结果** | 之前的对话内容完整保留 |

### TC-11-02 重启应用后恢复对话

| 项目 | 内容 |
|------|------|
| **前置条件** | 已有保存的对话历史 |
| **操作步骤** | 1. 关闭应用<br>2. 重新启动应用<br>3. 进入AI助手页面<br>4. 选择之前的场景和对话历史 |
| **预期结果** | 对话内容完整恢复，所有消息按原顺序显示 |

### TC-11-03 多条对话间切换数据隔离

| 项目 | 内容 |
|------|------|
| **前置条件** | 同一场景下有两个对话历史A和B |
| **操作步骤** | 1. 在对话A中发送消息<br>2. 切换到对话B<br>3. 再切换回对话A |
| **预期结果** | 对话A的内容不受对话B影响；两个对话的数据完全隔离 |

---

## TC-12 错误处理

### TC-12-01 API密钥无效

| 项目 | 内容 |
|------|------|
| **前置条件** | LLM配置中的API Key为无效值 |
| **操作步骤** | 1. 选择该LLM配置<br>2. 发送消息 |
| **预期结果** | 聊天区域显示错误提示（如认证失败）；不出现应用崩溃 |

### TC-12-02 网络不可达

| 项目 | 内容 |
|------|------|
| **前置条件** | LLM服务地址不可达（如错误的base_url） |
| **操作步骤** | 1. 选择该LLM配置<br>2. 发送消息 |
| **预期结果** | 显示网络连接错误信息；提示"发送消息失败"；已有对话数据不受影响 |

### TC-12-03 LLM返回错误响应

| 项目 | 内容 |
|------|------|
| **前置条件** | LLM服务正常但模型返回错误 |
| **操作步骤** | 1. 发送超长上下文触发token限制错误 |
| **预期结果** | 错误信息显示在聊天区域；提示具体错误原因（如context length exceeded） |

### TC-12-04 加载LLM配置失败

| 项目 | 内容 |
|------|------|
| **前置条件** | LLM配置文件损坏或格式错误 |
| **操作步骤** | 1. 打开AI助手页面 |
| **预期结果** | LLM模型下拉框加载失败时，提示"加载LLM配置失败"；不影响其他功能使用 |

### TC-12-05 加载场景失败

| 项目 | 内容 |
|------|------|
| **前置条件** | 场景配置文件损坏 |
| **操作步骤** | 1. 打开AI助手页面<br>2. 查看场景下拉框 |
| **预期结果** | 提示"加载场景失败"；其他功能正常 |

---

## TC-13 边界与异常

### TC-13-01 超长消息输入

| 项目 | 内容 |
|------|------|
| **前置条件** | 已完成配置 |
| **操作步骤** | 1. 在输入框输入超过5000字符的文本<br>2. 发送 |
| **预期结果** | 消息正常发送；AI正常回复（或因token限制返回错误提示）；应用无崩溃 |

### TC-13-02 快速连续发送消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已完成配置 |
| **操作步骤** | 1. 发送消息A<br>2. 在AI回复完成前再次尝试发送消息B |
| **预期结果** | 第二次发送被阻止（按钮处于loading/禁用状态）；需等待AI回复完成后才能发送新消息 |

### TC-13-03 特殊字符消息

| 项目 | 内容 |
|------|------|
| **前置条件** | 已完成配置 |
| **操作步骤** | 1. 输入包含特殊字符的消息，如 `<script>alert('xss')</script>`<br>2. 发送 |
| **预期结果** | 消息正常显示，不会被当作HTML执行；AI正常回复 |

### TC-13-04 对话历史文件不存在

| 项目 | 内容 |
|------|------|
| **前置条件** | 对话历史列表中有记录，但实际文件已被外部删除 |
| **操作步骤** | 1. 选择该对话历史 |
| **预期结果** | 提示"加载对话历史失败"或"初始化对话失败"；不崩溃 |

---

## 测试用例统计

| 模块 | 用例数 |
|------|--------|
| 入口与导航 | 4 |
| 场景选择 | 10 |
| LLM模型选择 | 3 |
| 对话历史管理 | 4 |
| 发送消息 | 8 |
| 流式响应 | 3 |
| 回退与重新生成 | 6 |
| Agent模式 | 6 |
| 编辑对话 | 4 |
| 原始数据 | 2 |
| 消息持久化 | 3 |
| 错误处理 | 5 |
| 边界与异常 | 4 |
| **合计** | **62** |
