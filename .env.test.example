# Qtian 测试配置示例
# 复制此文件为 .env.test 并填入实际的测试配置

# 测试工作目录路径（与实际运行时保持一致）
# 默认值为 testing/workspace，通常不需要修改
# 自定义路径示例：QTIAN_WORKSPACE=/custom/test/workspace
QTIAN_WORKSPACE=tests/resource/workspace

# 测试超时时间（毫秒）
TEST_TIMEOUT=30000

# 是否启用详细日志
TEST_VERBOSE_LOGGING=false
